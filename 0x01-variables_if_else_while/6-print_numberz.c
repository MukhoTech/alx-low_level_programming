#include <stdio.h>

/**
*main - A C program that prints digit numbers of base 10
*Return: Value 0 (Success)
*/

int main(void)
{
	int x;

	for (x = 0; x <= 9; x++)
		putchar(x);
	putchar('\n');

	return (0);
}
