#include "main.h"

/**
 * mul - multiply two integers
 * @x: first number to multply
 * @y: second number to multply with the first number
 * Return: the result of the multiplication
 */

int mul(int x, int y)
{
	return (x * y);
}
